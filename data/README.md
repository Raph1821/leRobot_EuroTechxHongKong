# data/

Static, non-runtime data files.

## What belongs here

- Medicine name lists (`medicine_names.json`)
- MediaPipe model files (`pose_landmarker_lite.task`)
- Example/test configs (`test_doses_api.json`)

Do NOT put runtime-generated files here (logs, runtime state, memory stores).
