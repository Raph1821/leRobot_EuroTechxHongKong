# core/

Shared utilities used across all CareAI modules.

## What belongs here

- Event logging (`event_log.py`)
- Shared camera frame buffer (`shared_frame.py`)
- Application mode enum (`modes.py`)
- Config loading helpers
- Common types and constants
