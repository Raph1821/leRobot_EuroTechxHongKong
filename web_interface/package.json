{
  "name": "so100-web-control",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Web-based 3D control interface for the SO-100 robot arm",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "fast-check": "^3.23.0",
    "happy-dom": "^20.9.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
