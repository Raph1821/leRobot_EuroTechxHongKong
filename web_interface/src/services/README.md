# Services

This directory will contain service modules:

- `ConnectionManager.ts` — WebSocket lifecycle and reconnection (Task 7.2)
- `SceneManager.ts` — Three.js scene setup, camera, ground plane (Task 7.6)
